use parsley::{get_text_from_step};
use txml::{Template};

// text injections without escaped text
// if style or script then don't escape text

fn build_template(template: Template) -> String {
    let templ = "".to_string();

    // stack across template
    // templates -> template builds

    
    templ
}
